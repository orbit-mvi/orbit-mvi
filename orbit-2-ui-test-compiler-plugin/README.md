# Orbit 2 UI testing compiler plugin

The module provides code to launch stub ContainerHosts.

## Including the module

```kotlin
testImplementation("com.babylon orbit2:orbit-ui-test-compiler-plugin:<latest-version>")
```
